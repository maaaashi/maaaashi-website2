datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}

model WebSite {
  id    Int     @id @default(autoincrement())
  key   String
  value String
}
