datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}

model AboutMe {
  id    Int     @id @default(autoincrement())
  text  String
}
